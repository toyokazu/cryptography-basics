<% content_for :title do -%>
  単一換字暗号 演習問題
<% end -%>
<h1>単一換字暗号 演習問題</h1>

<h2>復号化（暗号文を平文に戻す）</h2>

次の暗号文

<div class="plain"><%= @encrypted %></div>

を単一換字暗号で復号化せよ．
ただし，鍵は

<%= render 'simple_substitution_cipher/simple_substitution_table' %>

とする．

<%= form_tag(simple_substitution_cipher_exercises_2_path, :method => :post) do %>
  <%= text_field_tag(:answer, @answer, :size => @plain.length) %>
  <% @key.each do |k| %>
    <%= hidden_field_tag('key[]', k) %>
  <% end %>
  <%= submit_tag "解答する" %>
<% end %>
