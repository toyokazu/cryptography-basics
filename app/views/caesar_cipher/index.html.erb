<% content_for :title do -%>
  シーザー暗号
<% end -%>
<h1>シーザー暗号</h1>

<h2>概要</h2>

<p>シーザー暗号 (Caesar Cipher) は，ジュリアス・シーザー（ユリウス・カエサル）が使っていたといわれる暗号です．シーザーは紀元前100年ごろにローマで生まれ，活躍した武将です．</p>
<p>シーザー暗号では，平文で使われているアルファベットを，一定の文字数だけ「ずらす」ことによって暗号化を行います．シーザー暗号は日本語にも適用できますが，ここでは簡単のためアルファベットだけを用います．</p>

<h2>体験</h2>

<p>シーザー暗号を体験してみましょう．以下では便宜上，小文字 (a, b, c, ...) で平文のアルファベットを表記し，大文字 (A, B, C, ...) で暗号文のアルファベットを表記します．</p>
<p>シーザー暗号では「ずらす」文字数が暗号鍵に相当しています．現在の暗号鍵は<%= @key %>です．<p>
<%= form_tag(caesar_cipher_path, :method => :get) do %>
  暗号鍵を他の値に変更できます．<%= text_field_tag(:key, @key, :size => 2) %><br/>
  <%= submit_tag "設定" %>
<% end %>

<br/>

<h3>暗号化に用いる表</h3>
<p>上記の暗号鍵でアルファベットを変換した場合，どのような文字に変換されるかを示した表です．</p>
<%= render 'caesar_table' %>

<h3>暗号化の例</h3>

<p>上記の表を用いた場合の暗号化の例を示します．</p>

暗号化前：<%= @before %><br/>
暗号化後：<%= @after %>


<h2>問題に挑戦</h2>

<p>シーザー暗号の演習問題です．上記の表などを活用しながら，理解できたかどうか確認してみてください．</p>

<%= link_to '演習問題1: 暗号化（平文を暗号文にする）', caesar_cipher_exercises_1_path(params) %><br/>
<%= link_to '演習問題2: 復号化（暗号文を平文に戻す）', caesar_cipher_exercises_2_path(params) %><br/>
