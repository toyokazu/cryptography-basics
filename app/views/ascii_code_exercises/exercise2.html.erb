<h1>2進数 演習問題</h1>

<h2>問題 (2進数 => 文字列)</h2>

2進数 <div class="binary"><%= @binary_array.join(", ") %></div> は文字列で表すと（　　）になる．

<%= form_tag(ascii_code_exercises_2_path, :method => :post) do %>
  <%= text_field_tag(:answer, @answer, :size => @word.length) %>
  <%= submit_tag "解答する" %>
<% end %>
